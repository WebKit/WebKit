// This file is automatically generated from CSSProperties.json by the process-css-properties.py script. Do not edit it.

#pragma once

#include <WebCore/LayoutUnit.h>
#include <WebCore/StyleComputedStyle.h>
#include <WebCore/UsedStyleReferenceSize.h>
#include <optional>
#include <wtf/CheckedPtr.h>
#include <wtf/CheckedRef.h>

namespace WebCore {

class RenderElement;

class UsedStyleProperties {
public:
    const Style::ComputedStyle& computedStyle() const LIFETIME_BOUND { return m_computedStyle.get(); }

protected:
    // UsedStyle wraps a computed style and the renderer it describes, held as a pair. The
    // renderer is used for renderer-derived references (e.g. the containing block, see
    // resolveReferenceSize). const: a UsedStyle view never rebinds either member, so they
    // can be read without a stack-local guard.
    UsedStyleProperties(const Style::ComputedStyle& computedStyle LIFETIME_BOUND, const RenderElement* renderer LIFETIME_BOUND)
        : m_computedStyle(computedStyle)
        , m_renderer(renderer)
    {
    }

    // Resolves the reference a percentage value evaluates against (e.g. the containing
    // block inline size). Renderer-derived, so hand-written out-of-line in UsedStyle.cpp.
    LayoutUnit resolveReferenceSize(ReferenceSize) const;

    const CheckedRef<const Style::ComputedStyle> m_computedStyle;
    const CheckedPtr<const RenderElement> m_renderer;
};

} // namespace WebCore

