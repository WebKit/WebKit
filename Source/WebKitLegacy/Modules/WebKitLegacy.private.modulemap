framework module WebKitLegacy [system] {
  // Workaround for rdar://173516139: PrivateHeaders includes a header
  // "WorkAround173516139.h" that the build system regenerates whenever the
  // header list changes.
  umbrella "PrivateHeaders"

  explicit module * { export * }
}
