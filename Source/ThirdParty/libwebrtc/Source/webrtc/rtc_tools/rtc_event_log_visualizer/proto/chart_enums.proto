syntax = "proto3";
// Contains enums used as part of chart.proto
package webrtc.analytics;

message ChartStyle {
  enum Type {
    UNDEFINED = 0;
    LINE_CHART = 1;
    BAR_CHART = 2;
    LINE_STEP_CHART = 3;
    SCATTER_CHART = 4;
  };
};
