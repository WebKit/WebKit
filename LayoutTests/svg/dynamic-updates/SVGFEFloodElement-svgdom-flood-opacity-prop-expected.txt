SVG 1.1 dynamic update tests

Tests dynamic updates of the 'flood-opacity' property of the SVGFEFloodElement object

On success, you will see a series of "PASS" messages, followed by "TEST COMPLETE".


FAIL parseFloat(document.defaultView.getComputedStyle(floodElement).getPropertyValue('flood-opacity')).toFixed(1) should be 0.1. Was 1.0.
PASS parseFloat(document.defaultView.getComputedStyle(floodElement).getPropertyValue('flood-opacity')).toFixed(1) is "0.8"
PASS successfullyParsed is true

TEST COMPLETE

