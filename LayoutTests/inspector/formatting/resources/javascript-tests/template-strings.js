`template string`
`${1+1}`
`${  1  +  1  }`
`before ${1+1} after`
`before ${`nested ${1+1}`} after`
`before ${`nested ${1+1}`} after`

tag`tagged template string`
tag `tagged template string` 
tag`before ${1+1} after`
