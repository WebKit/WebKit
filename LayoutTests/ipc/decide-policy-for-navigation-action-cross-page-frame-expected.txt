PASS if no crash. A WebContent process that sends DecidePolicyForNavigationActionAsync naming a different WebPageProxy's WebFrameProxy must be rejected by MESSAGE_CHECK before the foreign frame is touched.
