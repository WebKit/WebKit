The shadow host must not be matched by a rule-hash bucket shortcut or a compiled selector, which do not implement featureless matching. Same as host-featureless-matching.html with the selector JIT disabled.

On success, you will see a series of "PASS" messages, followed by "TEST COMPLETE".


Selectors the JIT compiles that match a div, but must not match the featureless shadow host
PASS hostMatches("*") is false
PASS hostMatches("div") is false
PASS hostMatches(".host") is false
PASS hostMatches(":not(span)") is false
PASS hostMatches(":not(aside)") is false
PASS hostMatches(":not(.foo)") is false
PASS hostMatches(":not(:hover)") is false

Selectors that compile to an always-false stub, so the compiler never reaches the :host
PASS hostMatches("[a^='']:host") is false
PASS hostMatches(":nth-child(0):host") is false
PASS hostMatches(":nth-child(0 of :host)") is false

Controls: the host still matches what it is allowed to
PASS hostMatches(":host") is true
PASS hostMatches(":not(:not(:host))") is true
PASS successfullyParsed is true

TEST COMPLETE

