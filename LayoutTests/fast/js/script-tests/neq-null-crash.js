description("This test for a random crash when generated bytecodes include 'op_neq_null'.");

function crush() {
    return (document.all != null && undefinedVar);
}

for (var i = 1; i < 100; i++) {
    shouldBeFalse("crush()");
}

