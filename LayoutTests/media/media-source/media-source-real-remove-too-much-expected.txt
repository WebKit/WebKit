Tests SourceBuffer.remove() with real MP4 data. Removes [0, 4) and verifies removal extends to the sync frame boundary at PTS=4.

RUN(video.src = URL.createObjectURL(source))
EVENT(sourceopen)
RUN(sourceBuffer = source.addSourceBuffer('video/mp4; codecs="avc1.4d401e"'))
EVENT(updateend)
EVENT(updateend)
RUN(sourceBuffer.remove(0, 4))
EVENT(updateend)
EXPECTED (sourceBuffer.buffered.start(0) == '4') OK
END OF TEST

