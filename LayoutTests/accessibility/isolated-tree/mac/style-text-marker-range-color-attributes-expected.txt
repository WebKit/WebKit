This test ensures AXStyleTextMarkerRangeForTextMarker splits an intra-paragraph style range on text color and on background color, and that two identically styled runs separated by differently styled ones stay separate ranges.

PASS: characterAt(inRed) === 'e'
PASS: styleRangeAt(inRed) === 'red'
PASS: styleRangeAt(inBack) === 'back'
PASS: styleRangeAt(inPlain) === 'plain'
PASS: styleRangeAt(inTail) === 'tail'

PASS successfullyParsed is true

TEST COMPLETE

