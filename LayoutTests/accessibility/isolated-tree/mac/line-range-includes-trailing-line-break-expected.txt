This tests that a line's text marker range includes its trailing line break, and that a line
that ends its block, which has no trailing line break, ends with its own text.

PASS: editable.textMarkerRangeLength(firstLineRange) === 12
PASS: editable.stringForTextMarkerRange(firstLineRange) === 'First line.\n' === true
PASS: editable.textMarkerRangeLength(lastLineRange) === 12
PASS: editable.stringForTextMarkerRange(lastLineRange) === 'Second line.' === true
PASS: editable.endTextMarkerForTextMarkerRange(lastLineRange).isEqual(endMarker) === true
PASS: editable.textMarkerRangeLength(fullRange) === 24

PASS successfullyParsed is true

TEST COMPLETE
First line.
Second line.
After the editable.
