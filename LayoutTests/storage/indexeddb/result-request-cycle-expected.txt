Verify that IDBRequest is not leaked when there is a reference cycle for result attribute

On success, you will see a series of "PASS" messages, followed by "TEST COMPLETE".


indexedDB = self.indexedDB || self.webkitIndexedDB || self.mozIndexedDB || self.msIndexedDB || self.OIndexedDB;

indexedDB.deleteDatabase(dbname)
indexedDB.open(dbname)

prepareDatabase():
db = event.target.result
store = db.createObjectStore('store')

onOpen():
db = event.target.result
tx = db.transaction('store')
store = tx.objectStore('store')
Issue 100 get requests for 'key1'.
getRequest2 = store.get('key2')
getRequest3 = store.get('key2')

():
Verify that the request's result can be accessed lazily:
gc()
PASS results[0].value is "value1"
PASS getRequest2.result.value is "value2"
Ensure requests are not released while their results are referenced.
gc()
PASS anyCollected(requestRefs) is false
PASS anyCollected(resultRefs) is false
Ensure requests and results are released once the results are dropped.
gc()
PASS anyCollected(requestRefs) is true
PASS anyCollected(resultRefs) is true
PASS successfullyParsed is true

TEST COMPLETE

