Verify that IDBCursor is not leaked when there is a reference cycle for value attribute

On success, you will see a series of "PASS" messages, followed by "TEST COMPLETE".


indexedDB = self.indexedDB || self.webkitIndexedDB || self.mozIndexedDB || self.msIndexedDB || self.OIndexedDB;

indexedDB.deleteDatabase(dbname)
indexedDB.open(dbname)

prepareDatabase():
db = event.target.result
store = db.createObjectStore('store')

onOpen():
db = event.target.result
tx = db.transaction('store')
store = tx.objectStore('store')
Open 100 cursors.
getRequest = store.get('key')
PASS getRequest.result.name is "value"
Give every cursor a value that refers back to the cursor.
PASS cursors.length is cursorCount
PASS values[0].name is "value"
Ensure cursors are not released while their values are referenced.
PASS anyCollected(cursorRefs) is false
Ensure cursors and values are released once the values are dropped.
PASS collected is true
PASS successfullyParsed is true

TEST COMPLETE

