Verifies that 'user-select' supersedes '-webkit-user-select' once the feature flag and the linked-on-or-after behavior are both enabled.

On success, you will see a series of "PASS" messages, followed by "TEST COMPLETE".


'none' applies to whichever spelling was used, and 'user-select' wins when both are present.
PASS selectedText.includes("webkitNoneHost") is false
PASS selectedText.includes("unprefixedNoneHost") is false
PASS selectedText.includes("bothSpellingsChild") is false

'-webkit-user-select' still applies when 'user-select' was not used, and its 'auto' still means 'text'.
PASS selectedText.includes("webkitTextChild") is true
PASS selectedText.includes("webkitAutoChild") is true

'user-select' overrides an inherited '-webkit-user-select', and its 'auto' resolves against the parent's used value.
PASS selectedText.includes("unprefixedTextChild") is true
PASS selectedText.includes("unprefixedAutoChild") is false
PASS successfullyParsed is true

TEST COMPLETE

