Verifies that 'user-select' has no effect at all while the feature flag is disabled, leaving '-webkit-user-select' as the only property that decides what is selectable.

On success, you will see a series of "PASS" messages, followed by "TEST COMPLETE".


Only the prefixed 'none' applies: the unprefixed one is inert, and the prefixed value wins when both are present.
PASS selectedText.includes("webkitNoneHost") is false
PASS selectedText.includes("unprefixedNoneHost") is true
PASS selectedText.includes("bothSpellingsChild") is true

'-webkit-user-select' applies as it always has, and its 'auto' still means 'text'.
PASS selectedText.includes("webkitTextChild") is true
PASS selectedText.includes("webkitAutoChild") is true

'user-select' cannot override an inherited '-webkit-user-select', and cannot restrict its own descendants either.
PASS selectedText.includes("unprefixedTextChild") is false
PASS selectedText.includes("unprefixedAutoChild") is true
PASS successfullyParsed is true

TEST COMPLETE

