CONSOLE MESSAGE: Unrecognized Content-Security-Policy directive 'require-trusted-types-for'.


FAIL insertAdjacentHTML with html assigned via policy (successful HTML transformation). undefined is not an object (evaluating 'win.trustedTypes.createPolicy')
FAIL `insertAdjacentHTML(string)` throws. assert_throws_js: function "_ => {
      d.insertAdjacentHTML('beforebegin', "<p>Fail</p>");
    }" did not throw
FAIL `insertAdjacentHTML(null)` throws. assert_throws_js: function "_ => {
      d.insertAdjacentHTML('beforebegin', null);
    }" did not throw
FAIL `insertAdjacentHTML(string)` assigned via default policy (successful HTML transformation). undefined is not an object (evaluating 'window.trustedTypes.createPolicy')
PASS `insertAdjacentHTML(null)` assigned via default policy does not throw.

