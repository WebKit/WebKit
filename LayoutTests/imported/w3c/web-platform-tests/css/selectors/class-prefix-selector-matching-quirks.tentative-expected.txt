
PASS this test runs in quirks mode
PASS sanity check: a regular class selector matches case-insensitively in quirks mode, regardless of the case used in the selector or the markup
PASS class="foo-bar" matches .foo-* in quirks mode
PASS class="FOO-bar" matches .foo-* in quirks mode (the class attribute value itself is ASCII-lowercased by the quirks mode class-matching quirk)
PASS class="FOO-bar" does not match .FOO-* in quirks mode: unlike a regular class selector, the class prefix selector itself is never case-folded, so writing it in a different case than the (quirks-mode-lowercased) class attribute never matches

