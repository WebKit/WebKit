2d.shadow.image.section

Shadows are not drawn for areas outside image source rectangles


FAIL Shadows are not drawn for areas outside image source rectangles null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

