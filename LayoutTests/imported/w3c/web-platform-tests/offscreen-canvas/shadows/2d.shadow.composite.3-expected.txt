2d.shadow.composite.3

Areas outside shadows are drawn correctly with destination-out


FAIL Areas outside shadows are drawn correctly with destination-out null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

