2d.shadow.gradient.alpha

Shadows are drawn correctly for partially-transparent gradient fills


FAIL Shadows are drawn correctly for partially-transparent gradient fills null is not an object (evaluating 'ctx.createLinearGradient')

