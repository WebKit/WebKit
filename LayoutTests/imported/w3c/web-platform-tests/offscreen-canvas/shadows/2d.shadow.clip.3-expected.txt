2d.shadow.clip.3

Shadows of clipped shapes are still drawn within the clipping region


FAIL Shadows of clipped shapes are still drawn within the clipping region null is not an object (evaluating 'ctx.fillStyle = '#f00'')

