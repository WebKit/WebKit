2d.path.stroke.prune.rect

Zero-length line segments from rect and strokeRect are removed before stroking


FAIL Zero-length line segments from rect and strokeRect are removed before stroking null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

