2d.path.stroke.prune.line

Zero-length line segments from lineTo are removed before stroking


FAIL Zero-length line segments from lineTo are removed before stroking null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

