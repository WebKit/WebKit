2d.path.stroke.prune.closed

Zero-length line segments from closed paths are removed before stroking


FAIL Zero-length line segments from closed paths are removed before stroking null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

