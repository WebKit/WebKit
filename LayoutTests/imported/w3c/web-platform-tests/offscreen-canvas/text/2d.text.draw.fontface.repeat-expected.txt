2d.text.draw.fontface.repeat

Draw with the font immediately, then wait a bit until and draw again. (This crashes some version of WebKit.)


FAIL Draw with the font immediately, then wait a bit until and draw again. (This crashes some version of WebKit.) null is not an object (evaluating 'ctx.fillStyle = '#f00'')

