

FAIL A sandboxed iframe cannot navigate its parent via its own navigation object by using back() assert_array_equals: lengths differ, expected array ["committed", "finished"] length 2, got ["todo"] length 1

