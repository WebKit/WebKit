2d.path.roundrect.1.radius.dompoint
Verify that when one radius is given to roundRect(), specified as a DOMPoint, it applies to all corners.
Actual output:

FAIL Verify that when one radius is given to roundRect(), specified as a DOMPoint, it applies to all corners. ctx.roundRect is not a function. (In 'ctx.roundRect(0, 0, 100, 50, [new DOMPoint(40, 20)])', 'ctx.roundRect' is undefined)

