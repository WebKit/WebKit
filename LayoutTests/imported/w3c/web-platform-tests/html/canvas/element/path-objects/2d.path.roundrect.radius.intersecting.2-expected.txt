2d.path.roundrect.radius.intersecting.2
Check that roundRects with intersecting corner arcs are rendered correctly.
Actual output:

FAIL Check that roundRects with intersecting corner arcs are rendered correctly. ctx.roundRect is not a function. (In 'ctx.roundRect(0, 0, 100, 50, [1000, 1000, 1000, 1000])', 'ctx.roundRect' is undefined)

