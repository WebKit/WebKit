
PASS save()/restore() restores lineWidth, for a canvas of size (300, 150).
PASS save() does not modify lineWidth, for a canvas of size (300, 150).
PASS save()/restore() restores lineWidth, for a canvas of size (0, 0).
PASS save() does not modify lineWidth, for a canvas of size (0, 0).

