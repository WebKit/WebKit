2d.path.roundrect.radius.toomany

Check that roundRect throws an IndeSizeError if radii has more than four items.


PASS Check that roundRect throws an IndeSizeError if radii has more than four items.

