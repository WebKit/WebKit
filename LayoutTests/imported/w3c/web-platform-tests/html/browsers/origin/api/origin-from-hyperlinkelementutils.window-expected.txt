
PASS Origin.from(<a>) throws.
PASS Origin.from(<area>) throws.
PASS Origin.from(SVG <a>) throws.
PASS Origin.from(MathML <a>) throws.
PASS Origin.from(<a href="about:blank">) returns an opaque origin.
PASS Origin.from(<area href="about:blank">) returns an opaque origin.
PASS Origin.from(SVG <a href="about:blank">) returns an opaque origin.
PASS Origin.from(SVG <a xlink:href="about:blank">) returns an opaque origin.
FAIL Origin.from(MathML <a href="about:blank">) returns an opaque origin. Type error
PASS Origin.from(<a href="data:text/plain,opaque">) returns an opaque origin.
PASS Origin.from(<area href="data:text/plain,opaque">) returns an opaque origin.
PASS Origin.from(SVG <a href="data:text/plain,opaque">) returns an opaque origin.
PASS Origin.from(SVG <a xlink:href="data:text/plain,opaque">) returns an opaque origin.
FAIL Origin.from(MathML <a href="data:text/plain,opaque">) returns an opaque origin. Type error
PASS Origin.from(<a href="weird-protocol:whatever">) returns an opaque origin.
PASS Origin.from(<area href="weird-protocol:whatever">) returns an opaque origin.
PASS Origin.from(SVG <a href="weird-protocol:whatever">) returns an opaque origin.
PASS Origin.from(SVG <a xlink:href="weird-protocol:whatever">) returns an opaque origin.
FAIL Origin.from(MathML <a href="weird-protocol:whatever">) returns an opaque origin. Type error
PASS Origin.from(<a href="weird-hierarchical-protocol://host/path?etc">) returns an opaque origin.
PASS Origin.from(<area href="weird-hierarchical-protocol://host/path?etc">) returns an opaque origin.
PASS Origin.from(SVG <a href="weird-hierarchical-protocol://host/path?etc">) returns an opaque origin.
PASS Origin.from(SVG <a xlink:href="weird-hierarchical-protocol://host/path?etc">) returns an opaque origin.
FAIL Origin.from(MathML <a href="weird-hierarchical-protocol://host/path?etc">) returns an opaque origin. Type error
PASS Origin.from(<a href="blob:weird-protocol:whatever">) returns an opaque origin.
PASS Origin.from(<area href="blob:weird-protocol:whatever">) returns an opaque origin.
PASS Origin.from(SVG <a href="blob:weird-protocol:whatever">) returns an opaque origin.
PASS Origin.from(SVG <a xlink:href="blob:weird-protocol:whatever">) returns an opaque origin.
FAIL Origin.from(MathML <a href="blob:weird-protocol:whatever">) returns an opaque origin. Type error
PASS Origin.from(<a href="blob:weird-hierarchical-protocol://host/path?etc">) returns an opaque origin.
PASS Origin.from(<area href="blob:weird-hierarchical-protocol://host/path?etc">) returns an opaque origin.
PASS Origin.from(SVG <a href="blob:weird-hierarchical-protocol://host/path?etc">) returns an opaque origin.
PASS Origin.from(SVG <a xlink:href="blob:weird-hierarchical-protocol://host/path?etc">) returns an opaque origin.
FAIL Origin.from(MathML <a href="blob:weird-hierarchical-protocol://host/path?etc">) returns an opaque origin. Type error
PASS Origin.from(<a href="http://site.example">) returns a tuple origin.
PASS Origin.from(<area href="http://site.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="http://site.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="http://site.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="http://site.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://site.example">) returns a tuple origin.
PASS Origin.from(<area href="https://site.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://site.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://site.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://site.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://site.example:123">) returns a tuple origin.
PASS Origin.from(<area href="https://site.example:123">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://site.example:123">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://site.example:123">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://site.example:123">) returns a tuple origin. Type error
PASS Origin.from(<a href="http://sub.site.example">) returns a tuple origin.
PASS Origin.from(<area href="http://sub.site.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="http://sub.site.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="http://sub.site.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="http://sub.site.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://sub.site.example">) returns a tuple origin.
PASS Origin.from(<area href="https://sub.site.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://sub.site.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://sub.site.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://sub.site.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://sub.site.example:123">) returns a tuple origin.
PASS Origin.from(<area href="https://sub.site.example:123">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://sub.site.example:123">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://sub.site.example:123">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://sub.site.example:123">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://xn--mlauted-m2a.example">) returns a tuple origin.
PASS Origin.from(<area href="https://xn--mlauted-m2a.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://xn--mlauted-m2a.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://xn--mlauted-m2a.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://xn--mlauted-m2a.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="ftp://ftp.example">) returns a tuple origin.
PASS Origin.from(<area href="ftp://ftp.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="ftp://ftp.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="ftp://ftp.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="ftp://ftp.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="ws://ws.example">) returns a tuple origin.
PASS Origin.from(<area href="ws://ws.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="ws://ws.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="ws://ws.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="ws://ws.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="wss://wss.example">) returns a tuple origin.
PASS Origin.from(<area href="wss://wss.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="wss://wss.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="wss://wss.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="wss://wss.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://trailing.slash/">) returns a tuple origin.
PASS Origin.from(<area href="https://trailing.slash/">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://trailing.slash/">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://trailing.slash/">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://trailing.slash/">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://user:pass@site.example">) returns a tuple origin.
PASS Origin.from(<area href="https://user:pass@site.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://user:pass@site.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://user:pass@site.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://user:pass@site.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://has.a.port:1234/and/path">) returns a tuple origin.
PASS Origin.from(<area href="https://has.a.port:1234/and/path">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://has.a.port:1234/and/path">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://has.a.port:1234/and/path">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://has.a.port:1234/and/path">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://ümlauted.example">) returns a tuple origin.
PASS Origin.from(<area href="https://ümlauted.example">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://ümlauted.example">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://ümlauted.example">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://ümlauted.example">) returns a tuple origin. Type error
PASS Origin.from(<a href="blob:https://example.com/some-guid">) returns a tuple origin.
PASS Origin.from(<area href="blob:https://example.com/some-guid">) returns a tuple origin.
PASS Origin.from(SVG <a href="blob:https://example.com/some-guid">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="blob:https://example.com/some-guid">) returns a tuple origin.
FAIL Origin.from(MathML <a href="blob:https://example.com/some-guid">) returns a tuple origin. Type error
PASS Origin.from(<a href="ftp://example.com/">) returns a tuple origin.
PASS Origin.from(<area href="ftp://example.com/">) returns a tuple origin.
PASS Origin.from(SVG <a href="ftp://example.com/">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="ftp://example.com/">) returns a tuple origin.
FAIL Origin.from(MathML <a href="ftp://example.com/">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://example.com/path?query#fragment">) returns a tuple origin.
PASS Origin.from(<area href="https://example.com/path?query#fragment">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://example.com/path?query#fragment">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://example.com/path?query#fragment">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://example.com/path?query#fragment">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://127.0.0.1/">) returns a tuple origin.
PASS Origin.from(<area href="https://127.0.0.1/">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://127.0.0.1/">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://127.0.0.1/">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://127.0.0.1/">) returns a tuple origin. Type error
PASS Origin.from(<a href="https://[::1]/">) returns a tuple origin.
PASS Origin.from(<area href="https://[::1]/">) returns a tuple origin.
PASS Origin.from(SVG <a href="https://[::1]/">) returns a tuple origin.
PASS Origin.from(SVG <a xlink:href="https://[::1]/">) returns a tuple origin.
FAIL Origin.from(MathML <a href="https://[::1]/">) returns a tuple origin. Type error

