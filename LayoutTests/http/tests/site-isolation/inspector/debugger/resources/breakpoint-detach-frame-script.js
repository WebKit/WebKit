// report() is defined by the page that loads this script.
// setBreakpointInFrameScript() sets a breakpoint on the report() line by line number; keep them in sync.
function functionWithBreakpoint()
{
    report("iframe: breakpoint line ran");
}
