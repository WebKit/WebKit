Dragging a selection end grabber should extend the selection inside a cross-origin iframe, not leave it unchanged. The iframe is offset from the top-left so this also exercises the main-frame-to-subframe point conversion.

On success, you will see a series of "PASS" messages, followed by "TEST COMPLETE".


PASS initialSelection is "Hello"
PASS extendedSelection.length > initialSelection.length is true
PASS successfullyParsed is true

TEST COMPLETE

