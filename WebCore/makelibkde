#!/bin/sh
SUBLIBS=`find src/kdelibs -name 'lib*.a' -print`
LIBRARY=libkde.a

mkdir -p .objects
cd .objects

for SUBLIB in $SUBLIBS; do
	ar x ../$SUBLIB
done

cd ..

rm -f $LIBRARY
ar cru $LIBRARY .objects/*.o
ranlib $LIBRARY

rm -rf .objects
