namespace="WML"
namespaceURI="http://www.wapforum.org/DTD/wml_1.1.xml"
guardFactoryWith="ENABLE(WML)"

a
access
anchor
br interfaceName=WMLBRElement
card
do
go
noop
onevent interfaceName=WMLOnEventElement
p
postfield
prev
refresh
setvar
template
timer

#if 0
# These elements don't need a correponsponding WML*Element class.
# b, big, em, i, small, strong, u
#endif

#if 0
# These elements have a corresponding HTML*Element class, that we want to share code with.
# fieldset, head, img, input, meta, optgroup, option, select, table, td, tr
#endif
