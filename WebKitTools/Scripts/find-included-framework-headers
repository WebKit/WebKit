#!/bin/sh

for framework in $*; do
    echo -e "\n$framework\n=================="
    for ext in cpp h; do
        find . -name "*.$ext" -exec grep $framework {} ';' | grep include | sed -e 's|.*/\(.*\.h\).*|\1|'
    done | sort | uniq
done
