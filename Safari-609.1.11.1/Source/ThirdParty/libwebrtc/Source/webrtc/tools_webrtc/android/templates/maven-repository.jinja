allprojects {
    repositories {
        maven {
            url  '{{ url }}'
            credentials {
                username '{{ username }}'
                password '{{ password }}'
            }
        }
    }
}
