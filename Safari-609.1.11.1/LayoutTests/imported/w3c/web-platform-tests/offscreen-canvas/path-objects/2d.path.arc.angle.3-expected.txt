2d.path.arc.angle.3

arc() wraps angles mod 2pi when anticlockwise and end > start+2pi


FAIL arc() wraps angles mod 2pi when anticlockwise and end > start+2pi null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

