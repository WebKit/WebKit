2d.path.stroke.scale2

Stroke line widths are scaled by the current transformation matrix


FAIL Stroke line widths are scaled by the current transformation matrix null is not an object (evaluating 'ctx.fillStyle = '#f00'')

