2d.path.arcTo.ensuresubpath.1

If there is no subpath, the first control point is added (and nothing is drawn up to it)


FAIL If there is no subpath, the first control point is added (and nothing is drawn up to it) null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

