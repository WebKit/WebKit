2d.path.stroke.prune.arc

Zero-length line segments from arcTo and arc are removed before stroking


FAIL Zero-length line segments from arcTo and arc are removed before stroking null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

