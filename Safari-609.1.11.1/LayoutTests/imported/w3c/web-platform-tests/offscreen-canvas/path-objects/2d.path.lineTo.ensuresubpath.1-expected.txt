2d.path.lineTo.ensuresubpath.1

If there is no subpath, the point is added and nothing is drawn


FAIL If there is no subpath, the point is added and nothing is drawn null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

