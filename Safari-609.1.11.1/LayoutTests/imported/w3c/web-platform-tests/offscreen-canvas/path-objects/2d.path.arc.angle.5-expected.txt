2d.path.arc.angle.5

arc() wraps angles mod 2pi when clockwise and start > end+2pi


FAIL arc() wraps angles mod 2pi when clockwise and start > end+2pi null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

