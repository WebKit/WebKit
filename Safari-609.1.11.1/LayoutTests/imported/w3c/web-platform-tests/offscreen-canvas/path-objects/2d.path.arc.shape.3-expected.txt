2d.path.arc.shape.3

arc() from 0 to -pi/2 does not draw anything in the wrong quadrant


FAIL arc() from 0 to -pi/2 does not draw anything in the wrong quadrant null is not an object (evaluating 'ctx.fillStyle = '#0f0'')

