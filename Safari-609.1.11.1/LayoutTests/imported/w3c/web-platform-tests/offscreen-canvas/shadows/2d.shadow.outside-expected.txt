2d.shadow.outside

Shadows of shapes outside the visible area can be offset onto the visible area


FAIL Shadows of shapes outside the visible area can be offset onto the visible area null is not an object (evaluating 'ctx.fillStyle = '#f00'')

