2d.imageData.put.unchanged

putImageData(getImageData(...), ...) has no effect


FAIL putImageData(getImageData(...), ...) has no effect null is not an object (evaluating 'ctx.fillStyle = 'rgba(' + i + ',' + (Math.floor(i*1.5) % 256) + ',' + (Math.floor(i*23.3) % 256) + ',' + (i/256) + ')'')

