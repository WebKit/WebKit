2d.text.draw.baseline.top

textBaseline top is the top of the em square (not the bounding box)


FAIL textBaseline top is the top of the em square (not the bounding box) null is not an object (evaluating 'ctx.font = '50px CanvasTest'')

