2d.text.draw.baseline.bottom

textBaseline bottom is the bottom of the em square (not the bounding box)


FAIL textBaseline bottom is the bottom of the em square (not the bounding box) null is not an object (evaluating 'ctx.font = '50px CanvasTest'')

