2d.text.draw.align.right

textAlign right is the right of the last em square (not the bounding box)


FAIL textAlign right is the right of the last em square (not the bounding box) null is not an object (evaluating 'ctx.font = '50px CanvasTest'')

