2d.text.draw.space.collapse.end

Space characters at the end of a line are collapsed (per CSS)


FAIL Space characters at the end of a line are collapsed (per CSS) null is not an object (evaluating 'ctx.font = '50px CanvasTest'')

