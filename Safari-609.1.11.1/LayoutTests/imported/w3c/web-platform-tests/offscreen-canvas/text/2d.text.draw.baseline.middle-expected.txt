2d.text.draw.baseline.middle

textBaseline middle is the middle of the em square (not the bounding box)


FAIL textBaseline middle is the middle of the em square (not the bounding box) null is not an object (evaluating 'ctx.font = '50px CanvasTest'')

