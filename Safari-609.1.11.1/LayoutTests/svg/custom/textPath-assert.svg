<svg xmlns="http://www.w3.org/2000/svg">
  <path id="path" d="M10 30 l 200 0"/>
  <script>
    <![CDATA[
      if (window.testRunner)
          testRunner.dumpAsText();
      
      var svgns = "http://www.w3.org/2000/svg";
      var text = document.createElementNS(svgns, "text");
      var textpath = document.createElementNS(svgns, "textPath");
      textpath.setAttributeNS("http://www.w3.org/1999/xlink", "xlink:href", "#path");
      textpath.appendChild(document.createTextNode("PASSED if no ASSERT"));
      text.appendChild(textpath);
      document.documentElement.appendChild(text);
    ]]>
  </script>
</svg>
