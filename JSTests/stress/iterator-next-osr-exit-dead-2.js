//@ runDefault("--thresholdForOptimizeAfterWarmUp=100")
let obj = [0];

for (let i = 0; i < 10; i++) {
    let [a, b] = obj;
    obj.length = 2;

    for (let i = 0; i < 1e5; i++) {
        switch (Symbol) {
            case Symbol:
        }
    }
}

