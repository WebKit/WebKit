for (let i = 0; i < 10; i++) {
    for (let [a,b,c] of "NFKC") {
        const value = c >= c;
        try
        {
            Object.getPrototypeOf(value);
        }catch(e){}
        if (value) {}
    }
    const arr = new Int8Array(3314);
    for (const id in arr) {}
}
